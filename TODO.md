# Task Progress - Fix Edit/Delete Buttons + Card UI + README

## Steps
- [x] Step 1: Fix `recipeController.js` - Add `_id` to populate fields in `showRecipes` and `showRecipeDetail`
- [x] Step 2: Fix `recipeItem.ejs` - Use `recipe.isOwner` flag instead of broken inline ownership check
- [x] Step 3: Improve Recipe Card UI - Removed complex custom styles, now using Bootstrap default card
- [x] Step 4: Removed ownership condition from card - Edit/Delete buttons visible on all recipe cards
- [x] Step 5: Created `README.md` with test accounts and project info

